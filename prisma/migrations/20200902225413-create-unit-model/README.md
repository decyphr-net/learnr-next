# Migration `20200902225413-create-unit-model`

This migration has been generated by Aaron Sinnott at 9/2/2020, 7:54:13 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `learnr`.`Lesson` MODIFY `displayTitle` varchar(191) NOT NULL

ALTER TABLE `learnr`.`Module` MODIFY `displayTitle` varchar(191) NOT NULL

CREATE TABLE `learnr`.`Unit` (
`id` int  NOT NULL  AUTO_INCREMENT,
`title` varchar(191)  NOT NULL ,
`displayTitle` varchar(191)  NOT NULL ,
`contents` varchar(191)  NOT NULL ,
`lessonId` int  NOT NULL ,
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

ALTER TABLE `learnr`.`Unit` ADD FOREIGN KEY (`lessonId`) REFERENCES `learnr`.`Lesson`(`id`) ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200902200350-fix-typo-in-display-title..20200902225413-create-unit-model
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "mysql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -12,17 +12,27 @@
 model Module {
   id           Int      @id @default(autoincrement())
   title        String
-  displayTitle String?
+  displayTitle String
   description  String
   lessons      Lesson[]
 }
 model Lesson {
-  id           Int     @id @default(autoincrement())
+  id           Int    @id @default(autoincrement())
   title        String
-  displayTitle String?
+  displayTitle String
   description  String
-  module       Module  @relation(fields: [moduleId], references: [id])
+  module       Module @relation(fields: [moduleId], references: [id])
   moduleId     Int
+  units        Unit[]
 }
+
+model Unit {
+  id           Int    @id @default(autoincrement())
+  title        String
+  displayTitle String
+  contents     String
+  lesson       Lesson @relation(fields: [lessonId], references: [id])
+  lessonId     Int
+}
```


